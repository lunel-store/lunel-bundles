{
  "name": "lunel-bundles",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for bundling and minifying Lunel Bundles CDN assets.",
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
